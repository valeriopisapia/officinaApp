import { CustomNameFilterPipe } from './custom-name-filter.pipe';

describe('CustomNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
